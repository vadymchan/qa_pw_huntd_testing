# Instructions

- Following Playwright test failed.
- Explain why, be concise, respect Playwright best practices.
- Provide a snippet of code with the fix, if possible.

# Test info

- Name: account/candidate/account-settings/incorrectCurrentPassword.spec.ts >> Update candidate account settings >> User should see validation error when current password is incorrect
- Location: tests/account/candidate/account-settings/incorrectCurrentPassword.spec.ts:6:7

# Error details

```
Error: page.goto: Target page, context or browser has been closed
Call log:
  - navigating to "https://huntd.tech/settings/change-password", waiting until "load"

```

# Test source

```ts
  1  | import { expect, Locator, Page } from '@playwright/test';
  2  | import { graphqlWaitForResponse } from '@utils/playwright/graphqlWaitForResponse';
  3  | import { testStep } from '@utils/playwright/testStep';
  4  | 
  5  | export class BasePage {
  6  |   constructor(
  7  |     protected page: Page,
  8  |     protected path: string,
  9  |   ) {}
  10 | 
  11 |   protected async step<T>(title: string, stepToRun: () => Promise<T> | T): Promise<T> {
  12 |     return await testStep(title, stepToRun);
  13 |   }
  14 | 
  15 |   async open() {
> 16 |     await this.page.goto(this.path);
     |                     ^ Error: page.goto: Target page, context or browser has been closed
  17 |   }
  18 | 
  19 |   protected async clickAndWaitForOperation(locator: Locator, operationName: string) {
  20 |     await graphqlWaitForResponse(this.page, operationName, async () => {
  21 |       await locator.click();
  22 |     });
  23 |   }
  24 | 
  25 |   async assertOpened() {
  26 |     await this.step(`Assert '${this.path}' is opened`, async () => {
  27 |       await expect(this.page).toHaveURL(this.path);
  28 |     });
  29 |   }
  30 | }
  31 | 
```